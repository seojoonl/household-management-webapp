@import
	url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap')
	;

/* Base styles */
body, h1, h2, p, input, button {
	font-family: 'Playfair Display', serif;
	margin: 0;
	padding: 0;
	box-sizing: border-box;
}

header {
	display: flex;
	justify-content: space-between;
	align-items: center;
	background-color: #000;
	color: #fff;
	padding: 10px 20px;
	font-size: 24px;
}

header .wiki {
	margin-right: 10px;
}

header .household {
	margin-left: 650px;
	margin-right: auto;
}

header .logout {
	margin-left: 10px;
}

header .wiki a, header .logout a {
	color: inherit;
	text-decoration: none;
	font-size: 16px;
}

/* Header styles */
.header {
	padding: 20px;
	background: #99c1b9;
	color: white;
	text-align: center;
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
}

/* Styling specific for the household-info to align text as needed */
.household-info {
	text-align: center;
}

/* Task container adjustments for alignment */
.container {
	margin: 20px auto;
	border: 1px solid #ccc;
	padding: 10px;
	border-radius: 5px;
	background: #e8f0f4;
	max-width: 80%;
	display: flex;
	flex-direction: column;
	align-items: center;
}

.task {
	font-size: 16px;
	border-bottom: 1px solid #ccc;
	padding: 10px 0;
	display: grid;
	grid-template-columns: 2fr 2fr 1fr 1fr;
	/* Adjusted for balanced spacing */
	gap: 10px;
	width: 100%; /* Ensure full width within container */
	align-items: center; /* Align items vertically within each grid cell */
}

.task-name {
	font-weight: bold;
	overflow: hidden;
	text-overflow: ellipsis;
	white-space: nowrap; /* Prevent text from wrapping to the next line */
}

.assigned-to {
	font-style: italic;
	color: #555;
	text-align: left;
}

/* Button styles for alignment and modification */
button {
	background-color: #99c1b9;
	color: white;
	padding: 10px;
	border: none;
	border-radius: 5px;
	cursor: pointer;
	width: 100%; /* Make sure buttons fill their grid area */
}

button:hover {
	background-color: #2a8f99;
}

/* Input styles */
.task-manager-container {
	display: flex;
	flex-direction: column; /* Change to column for vertical alignment */
	justify-content: space-between;
	/* Ensures elements inside are spaced out evenly */
	padding: 0 50px; /* Padding to maintain consistency */
	margin: 0 auto; /* Center align the container */
	max-width: 80%; /* Prevents the container from extending full screen */
}

.popup {
	display: flex;
	align-items: center; /* Aligns items vertically in the center */
	margin-bottom: 20px;
	/* Provides space between popup and button container */
}

.buttonContainer {
	display: flex;
	align-items: center;
	justify-content: space-between;
	/* Ensures buttons are spaced out evenly */
}

.buttonContainer button, .popup button {
	flex-grow: 1;
	margin: 0 10px; /* Provides spacing between buttons */
	padding: 10px; /* Matches input padding for visual consistency */
	border-radius: 5px; /* Rounded corners like the input fields */
	background-color: #99c1b9; /* Light background, adjustable as needed */
	border: 1px solid #ccc; /* Matches input border style */
}

input[type="text"] {
	width: 100%; /* Adjust width to fit container */
	padding: 10px;
	margin: 10px 0;
	border-radius: 5px;
	border: 1px solid #ccc;
}

.close {
	cursor: pointer;
	/* Makes the close button behave like a clickable element */
	margin-right: 10px; /* Space before the input field */
}

.control-container {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	/* Ensures buttons are spaced out evenly */
	padding: 0 50px;
	/* Adjust the padding to get more space on the sides */
	margin: 0 auto; /* Center align the container */
	max-width: 80%;
	/* Prevents the container from extending full screen, adjust as needed */
}

.control-container button {
	flex-grow: 1;
	margin: 0 10px; /* Gives some space between the buttons */
}

/* Progress Bar Container */
#progressBarContainer {
	width: 100%;
	height: 20px;
	background-color: #f0f0f0;
	border-radius: 5px;
	overflow: hidden;
}

#progress {
	height: 100%;
	background-color: green;
	border-radius: 5px;
	text-align: center;
	color: white;
	line-height: 20px;
}